{% if errors|length > 0 %}
    <ul class="form-errors invalid-feedback list-unstyled my-1">
        {% for error in errors %}
            <li>{{ error.message }}</li>
        {% endfor %}
    </ul>
{% endif %}